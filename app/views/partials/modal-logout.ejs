<!-- Modal de confirmação -->
<section id="logoutModal" class="modal-overlay">
  <section class="modal-box">

    <svg xmlns="http://www.w3.org/2000/svg" height="60px" viewBox="0 -960 960 960" width="60px" fill="#bbb"><path d="M200-120q-33 0-56.5-23.5T120-200v-560q0-33 23.5-56.5T200-840h280v80H200v560h280v80H200Zm440-160-55-58 102-102H360v-80h327L585-622l55-58 200 200-200 200Z"/></svg>
    <p>Tem certeza de que deseja sair da sua conta?</p>
    <section class="modal-buttons">
      <button id="cancelLogout">Cancelar</button>
      <a href="/logout" class="confirm">Sair</a>
    </section>
</section>
</section>

<style>
    


/* Modal overlay (fundo escuro) */
.modal-overlay {
  font-family: "Josefin Sans", sans-serif;
  display: none;
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.5);
  justify-content: center;
  align-items: center;
  z-index: 1000;
}

/* Caixa do modal */
.modal-box {
  background: white;
  color: black;
  padding: 20px 30px;
  border-radius: 12px;
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
  max-width: 400px;
  text-align: center;
}

/* Botões */
.modal-buttons {
  display: flex;
  justify-content: center;
  gap: 20px;
  margin-top: 20px;
}

.modal-buttons button,
.modal-buttons a {
  padding: 10px 20px;
  border: none;
  border-radius: 8px;
  cursor: pointer;
  font-weight: bold;
  text-decoration: none;
}

/* Cancelar */
.modal-buttons button {
  background-color: transparent;
  border: 2px solid #707070;
  color: #707070;
  transition: all 0.3s ease;
  font-family: "Josefin Sans", sans-serif;
  font-size: medium;
}

.modal-buttons button:hover {
  background-color: #707070;
  color: white;
}

/* Confirmar (sair) */
.modal-buttons a.confirm {
  background-color: #d32f2f;
  color: white;
  font-family: "Josefin Sans", sans-serif;
  transition: all 0.3s ease;
  font-size: medium;

}

.modal-buttons a.confirm  a{
    padding: 0;
    margin: 0%;
}

.modal-buttons a.confirm:hover {
  background-color: #b71c1c;
}
</style>


<script>
    
      const openBtn = document.getElementById('openLogoutModal');
      const modallogout = document.getElementById('logoutModal');
      const cancelBtn = document.getElementById('cancelLogout');
    
      openBtn.addEventListener('click', () => {
        modallogout.style.display = 'flex';
      });
    
      cancelBtn.addEventListener('click', () => {
        modallogout.style.display = 'none';
      });
</script>